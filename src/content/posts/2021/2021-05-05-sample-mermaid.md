---
title: "A post that opts into Mermaid"
categories:
  - Cloud
tags:
  - azure
mermaid: true
---

Fixture post: sets `mermaid: true` to verify the optional boolean schema field.
The `<Mermaid>` rendering itself is wired up in a later milestone.
