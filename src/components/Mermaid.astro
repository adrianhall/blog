---
// Renders a pre element with class mermaid from a code string prop. The
// client-side mermaid script (see Base.astro) turns it into an SVG.
interface Props {
  code: string;
}
const { code } = Astro.props;
---

<pre class="mermaid">{code}</pre>
