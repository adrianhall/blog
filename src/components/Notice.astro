---
// Minimal callout component rendered as an aside with a notice--<type> class.
interface Props {
  type?: 'success' | 'warning' | 'info';
}
const { type = 'info' } = Astro.props;
---

<aside class={`notice notice--${type}`} role="note">
  <slot />
</aside>
